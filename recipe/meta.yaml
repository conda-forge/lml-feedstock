{% set name = "lml" %}
{% set version = "0.0.2" %}
{% set sha256 = "be66096d0c845bcabbbcaf772b2ba21e9cdb6e23e88b6cb32613fa8d89d535a9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - lml

about:
  home: https://github.com/chfw/lml.git
  license: BSD-3-Clause
  license_family: BSD
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE'
  summary: 'Load me later. A lazy loading plugin management system.'
  doc_url: http://lml.readthedocs.io/en/latest/
  dev_url: https://github.com/chfw/lml.git

extra:
  recipe-maintainers:
    - williamjamir
    - nicoddemus
